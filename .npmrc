# next-auth@5-beta declara un peer de nodemailer@^7, pero el proyecto usa
# nodemailer@8. Sin esta opción, `npm install` falla con ERESOLVE.
# Permite que el equipo instale con un simple `npm install`.
legacy-peer-deps=true
